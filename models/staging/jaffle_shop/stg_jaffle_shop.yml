version: 2
models:
  - name: raw_jaffle_shop__orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      # - name: status
      #   tests:
      #     - accepted_values:
      #       values: [ 'return_pending','shipped','completed','returned','placed']
